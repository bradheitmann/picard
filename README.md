# 🖖 PICARD v3.0 - Pure Bash

Multi-Agent Orchestration + Project State Awareness

**Pure bash scripts** - Simple, reliable, no dependencies.

Built on PSA foundation, adds:
- Message broker (agent-to-agent communication)
- PM-Dev-QA workflows (autonomous)
- Task management
- All via simple bash + SQLite

## Usage

```bash
picard              # Show dashboard
picard task create "Task name" high
picard send-message FROM TO "message"
picard workflow PROJECT TASK "spec"
```

Zero TypeScript. Zero node_modules. Just bash.

🖖 Make it so.
